const db = require('../database/dbConfig.js');
const Parents = require('./parents.js');

describe("parents.js", () => {
  describe("add()", () => {
    beforeEach(async () => {
      await db("parents").truncate();
    });

    await 
  });
});
